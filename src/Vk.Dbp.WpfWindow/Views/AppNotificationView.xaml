<UserControl x:Class="Vk.Dbp.WpfWindow.Views.AppNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vk.Dbp.WpfWindow.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="White"
             mc:Ignorable="d" 
             Width="350" Height="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="15,10">
            <Grid>
                <TextBlock Text="通知中心" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding UnreadCount, StringFormat='未读: {0}'}" 
                               Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="全部已读" Command="{Binding MarkAllAsReadCommand}"
                            Style="{StaticResource ButtonPrimary}" Padding="10,5" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="10,5" Background="#F5F5F5">
            <Button Content="刷新" Command="{Binding LoadCommand}" 
                    Style="{StaticResource ButtonDefault}" HorizontalAlignment="Right" Padding="15,5"/>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Background="White">
                            <Grid Margin="15,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="8" Height="8" CornerRadius="4" 
                                        Background="{StaticResource DangerBrush}" 
                                        Visibility="{Binding IsRead, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        VerticalAlignment="Center" Margin="0,0,10,0"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="13" 
                                               Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <TextBlock Text="{Binding Content}" FontSize="12" 
                                               Foreground="{StaticResource SecondaryTextBrush}"
                                               TextWrapping="Wrap" Margin="0,5,0,0" MaxHeight="60"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="{Binding Type}" FontSize="11" 
                                                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding CreatedTime, StringFormat='{}{0:MM-dd HH:mm}'}" 
                                                   FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Vertical" Margin="10,0,0,0">
                                    <Button Content="已读" Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonDefault}" 
                                            Padding="8,3" FontSize="11" Margin="0,0,0,5"
                                            Visibility="{Binding IsRead, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Hidden}"/>
                                    <Button Content="删除" Command="{Binding DataContext.DeleteNotificationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonDefault}" 
                                            Padding="8,3" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <hc:LoadingCircle Grid.Row="2" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                          VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
