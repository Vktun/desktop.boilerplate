<UserControl
    x:Class="Vk.Dbp.AccountModule.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource PrimaryBackgroundBrush}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="用户登录" FontSize="28" FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryTextBrush}" 
                       TextAlignment="Center" Margin="0,0,0,40"/>

            <!-- 用户名输入框 -->
            <TextBlock Text="用户名" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,5"/>
            <TextBox Padding="12,10" Margin="0,0,0,15"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource SecondaryBackgroundBrush}"
                     Foreground="{DynamicResource PrimaryTextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1" FontSize="14"/>

            <!-- 密码输入框 -->
            <TextBlock Text="密码" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Padding="12,10" Margin="0,0,0,25"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1" FontSize="14"/>

            <!-- 记住密码复选框 -->
            <CheckBox Content="记住密码" IsChecked="{Binding RememberPassword}"
                      Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,20"/>

            <!-- 登录按钮 -->
            <Button Content="登录" Padding="0,12" Margin="0,0,0,10"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox}"
                    Background="{DynamicResource PrimaryBrush}"
                    Foreground="White" FontSize="16" FontWeight="Bold"
                    Cursor="Hand"/>

            <!-- 错误消息显示 -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" 
                       TextWrapping="Wrap" Margin="0,10,0,0"
                       Visibility="{Binding ShowError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- 加载指示 -->
            <ProgressBar IsIndeterminate="True" Height="3" Margin="0,15,0,0"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
