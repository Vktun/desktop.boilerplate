<UserControl
    x:Class="Vk.Dbp.AccountModule.Views.UserManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:views="clr-namespace:Vk.Dbp.AccountModule.Views"
    prism:ViewModelLocator.AutoWireViewModel="True">
	
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
	
    <Grid Background="{DynamicResource PrimaryBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" Padding="15,10">
            <Grid>
                <TextBlock Text="用户管理" FontSize="20" FontWeight="Bold" 
                           Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="新增用户" Command="{Binding AddUserCommand}"
                            Style="{StaticResource ButtonPrimary}" Padding="10,0" Margin="0,0,10,0"/>
                    <Button Content="刷新" Command="{Binding LoadCommand}"
                            Style="{StaticResource ButtonDefault}" Padding="15,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="15,10" Background="{DynamicResource SecondaryBackgroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <hc:SearchBar Grid.Column="0" Width="300" HorizontalAlignment="Left"
                              Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                              Style="{StaticResource SearchBarExtend}"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="导出" Command="{Binding ExportCommand}"
                            Style="{StaticResource ButtonDefault}" Padding="15,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="15">
            <DataGrid ItemsSource="{Binding Users}" 
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      BorderBrush="{DynamicResource BorderBrush}"
                      BorderThickness="1"
                      Background="{DynamicResource PrimaryBackgroundBrush}"
                      RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                      AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                      Foreground="{DynamicResource PrimaryTextBrush}"
                      FontSize="13">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="120"/>
                    <DataGridTextColumn Header="真实姓名" Binding="{Binding RealName}" Width="120"/>
                    <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="电话" Binding="{Binding Phone}" Width="120"/>
                    <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="60"/>
                    <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
                    <DataGridTextColumn Header="最后登录" Binding="{Binding LastLoginTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
                    <DataGridTemplateColumn Header="操作" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="编辑" 
                                            Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonDefault}" 
                                            Padding="10,5" Margin="0,0,5,0" FontSize="12"/>
                                    <Button Content="删除" 
                                            Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonDanger}" 
                                            Padding="10,5" Margin="0,0,5,0" FontSize="12"/>
                                    <Button Content="重置密码" 
                                            Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonWarning}" 
                                            Padding="10,5" Margin="0,0,5,0" FontSize="12"/>
                                    <ToggleButton IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                                  Command="{Binding DataContext.EnableUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                  CommandParameter="{Binding}"
                                                  Style="{StaticResource ToggleButtonSwitch}"
                                                  Margin="5,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <hc:LoadingCircle Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                              VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>

        <Popup IsOpen="{Binding IsDialogOpen}" 
               Placement="Center" 
               AllowsTransparency="True"
               PopupAnimation="Fade"
               StaysOpen="True">
            <views:UserEditDialog DataContext="{Binding CurrentDialogViewModel}"/>
        </Popup>
    </Grid>
</UserControl>
