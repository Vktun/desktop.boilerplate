<UserControl
    x:Class="Vk.Dbp.AccountModule.Views.ChangePasswordView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource PrimaryBackgroundBrush}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="450">
            <TextBlock Text="修改密码" FontSize="28" FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryTextBrush}" 
                       TextAlignment="Center" Margin="0,0,0,40"/>

            <!-- 原密码 -->
            <TextBlock Text="原密码" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="OldPasswordBox" Padding="12,10" Margin="0,0,0,15"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1" FontSize="14"/>

            <!-- 新密码 -->
            <TextBlock Text="新密码" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="NewPasswordBox" Padding="12,10" Margin="0,0,0,15"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1" FontSize="14"/>

            <!-- 确认密码 -->
            <TextBlock Text="确认密码" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Padding="12,10" Margin="0,0,0,25"
                         Background="{DynamicResource SecondaryBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1" FontSize="14"/>

            <!-- 按钮组 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="修改" Padding="30,12" Margin="0,0,20,0"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="White" FontSize="14" FontWeight="Bold"
                        Cursor="Hand"/>
                <Button Content="取消" Padding="30,12"
                        Background="{DynamicResource SecondaryBrush}"
                        Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- 消息显示 -->
            <TextBlock Text="{Binding Message}" Foreground="{DynamicResource SuccessBrush}" 
                       TextWrapping="Wrap" Margin="0,20,0,0"
                       Visibility="{Binding ShowMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
